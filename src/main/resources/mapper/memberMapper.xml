<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hello.apiserver.api.member.mapper.MemberMapper">
    <select id="findMemberList" resultType="com.hello.apiserver.api.member.vo.MemberVo">
        SELECT id, name FROM member
    </select>
</mapper>